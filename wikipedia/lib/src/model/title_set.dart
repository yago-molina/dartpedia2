class TitleSet {
  // Construtor
  TitleSet({
    required this.canonico,
    required this.normalizado,
    required this.display,
  });

  // Atributos
  String canonico,
  String normalizado,
  String display,

  
}